import numpy as np
import pandas as pd
points={"x":[7,7,3,1],
      "y":[7,4,4,4],
      "target":["bad","bad","good","good"]}
df=pd.DataFrame(points)

target_point=(3,7)
k=3

df["distance"]=np.sqrt(np.square(df["x"]-target_point[0])+np.square(df["y"]-target_point[1]))
df1=df.sort_values(by="distance").head(k)
df2=df1.groupby('target') ['target'].count().sort_values(ascending=False).head(1)
print(df2)
df3=df2[['target']]
print(df3)























































































































































































































































































































































































































































































































































































































































































































































































































































